# Varyn

Varyn is a standalone Next.js application concept for a voice-enabled AI risk intelligence operating system.

## Local preview

```powershell
npm.cmd run build
powershell -ExecutionPolicy Bypass -File .\preview-varyn.ps1
```

The preview script chooses an available local port starting at `3200`.
